@using homework3_26.Data
@model HomeViewModel

@{
    ViewData["Title"] = "Home Page";
}

<html>
    <body>
<div b-k8y6yzo0ym class="container">
        <main b-k8y6yzo0ym role="main" class="pb-3">
  
            
<div class="row">
    <div class="col-md-8 offset-md-2" style="margin-top:100px">

        @foreach(Question q in Model.Questions)
                    {
                        <div class="card card-body bg-light">
                            <h4>
                                <a href="/home/viewquestion?id=@q.Id">
                                    @q.Title
                                </a>
                            </h4>
                            <div>
                                <span>Tags:</span>

                                <span>
                                        @String.Join(", ", q.QuestionsTags.Select(q => q.Tag.Name))                                  
                                </span>
                            </div>
                            <div style="margin-top: 10px;">
                                @(q.Description.Length < 200 ? q.Description : q.Description.Substring(0, 200) + ".....")
                            </div>
                            <br />
                            <span>@q.Answers.Count.ToString() answer(s)</span>
                        </div>
                    }           
    </div>
</div>


        </main>
    </div>
    
</body>
</html>